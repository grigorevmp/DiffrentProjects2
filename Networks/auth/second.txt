Какое минимальное количество пакетов надо перехватить между NAS-сервером и RADIUS-сервером, что бы организовать эффективный подбор разделяемого между ними секрета методом грубой силы (bruteforce), если заведомо известно, что процесс аутентификации пройден успешно?

- Один

MD5(CODE, ID, LENGTH, RA, Attr, SECRET) - 4 параметра отсюда требуют пакета A-A
Secret находится перебором.
Не смотря на то, что в коде я использовал данные 2 пакета, можно обойтись одним и просто перебрать RequestAuth брутфорсом из первого пакета.
Это будет намного дольше. Но сработает.

Какое минимальное количество пакетов надо перехватить между NAS-сервером и RADIUS-сервером, что бы организовать эффективный подбор пароля пользователся методом грубой силы (bruteforce), при использовании EAP-MD5 (разделяемый секрет нам не известен, аутентификация прошла успешно)?

- два последовательных пакета, идущие в разные стороны (первый - от RADIUS к NAS, второй - от NAS к RADIUS)

Требуется 2 пакета - хэшируются пароль, id и аутентификатор одного
Затем результат сравнивается с аутентификтором второго